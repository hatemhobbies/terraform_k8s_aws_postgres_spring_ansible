# Create a POD user

The pod user's role allows for **create,list,get,update,delete** pods
in the **project** namespace

**usage:**

**Via scripts:**

```sh
sh ./create-certificate.sh
sh ./rbac_apply.sh
# To create user's config and test using kubectl
sh ./create-pod-admin-config.sh
```
