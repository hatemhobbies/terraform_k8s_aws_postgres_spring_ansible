apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallet-service-postgres-data-persisent-volume-claim
spec:
  volumeName: wallet-service-postgres-data-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
