apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallet-service-source-code-persisent-volume-claim
spec:
  volumeName: wallet-service-source-code-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
